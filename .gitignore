# reference / cloned repos (not part of the tool)
/resources/

# per-project runtime (created by `bw init` in a product project)
# NOTE: _bewater/ and _bewater-output/ are tracked in this repo on purpose,
# but backup files, lock files, and tmp files are excluded.
_bewater/.backup-*
_bewater/.bw-lock
_bewater/.tmp-*

# misc / unrelated
/Frog:f212\ General/
.DS_Store

# python (bw CLI + tests)
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*

# claude deployment target (source of truth is src/skills/)
.claude/skills/
.claude/settings.local.json
