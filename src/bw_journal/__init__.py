"""bw-journal — a personal decision journal CLI (dogfood experiment EXP-001).

Thin wrapper around the ``bw`` assumption-ledger runtime. Provides a
user-friendly "git for decisions" workflow: ``log``, ``status``, ``diff``.

Telemetry: opt-in anonymous usage ping on ``log`` and ``status`` commands.
See ``cli.py`` for command handlers.
"""

__version__ = "0.1.0"
